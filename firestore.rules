rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default: deny all
    match /{document=**} {
      allow read, write: if false;
    }

    // Add collection-specific rules here.
    // See CLAUDE.md Security Baseline for patterns.
  }
}
